account_id = "..."
compatibility_date = "2022-06-18"
main = "build/worker/shim.mjs"

[build]
command = "cargo install worker-build && worker-build --release"

[env.production]
name = "signalupdates-bot"
vars = { USER_ID = "...", TOPIC_ID_OVERRIDE = "", DRY_RUN = "", ENABLED_PLATFORMS = "aid", DISCORD_UPDATES_MENTION_ROLE = "...", DISCORD_ERRORS_MENTION_ROLE = "..." }
kv_namespaces = [{ binding = "STATE", id = "..." }]
workers_dev = false
triggers = { crons = ["*/10 * * * *"] }

[env.staging]
name = "signalupdates-bot-staging"
vars = { USER_ID = "...", TOPIC_ID_OVERRIDE = "...", DRY_RUN = "...", ENABLED_PLATFORMS = "aid", DISCORD_UPDATES_MENTION_ROLE = "...", DISCORD_ERRORS_MENTION_ROLE = "..." }
kv_namespaces = [{ binding = "STATE", id = "..." }]
workers_dev = true
